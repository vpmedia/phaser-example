{
  "name": "@vpmedia/phaser-example",
  "version": "1.0.0",
  "description": "@vpmedia/phaser example",
  "source": "src/index.html",
  "scripts": {
    "prestart": "rm -rf dist",
    "start": "parcel",
    "prebuild": "rm -rf dist",
    "build": "parcel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andras Csizmadia <andras@vpmedia.hu> (www.vpmedia.hu)",
  "license": "MIT",
  "dependencies": {
    "@vpmedia/phaser": "^1.0.2"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "devDependencies": {
    "parcel": "^2.7.0",
    "process": "^0.11.10"
  }
}
